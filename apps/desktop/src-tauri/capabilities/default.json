{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the local Video2Notes window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
